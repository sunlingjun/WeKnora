# 开源贡献实施计划：三条独立 PR

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从干净 `upstream/main` 向 Tencent/WeKnora 提交三个独立 PR：Redis Cluster、SvgIcon 核心、色值接入（绿系）。

**Architecture:** 本地源真相为 Tag `nxin-v0.6.3-merge.1`；每条贡献分支从 `upstream/main` 新建，手工移植可上游化切片，不夹带 CAS/农信蓝板/离线护栏。

**Tech Stack:** Go（go-redis/asynq/Helm）、Vue 3 + TDesign CSS 变量。

**依据:** [开源贡献方案](./2026-07-15-开源贡献方案-Redis集群-SvgIcon-色值接入.md)、[本地合并版收口](./2026-07-15-本地v0.6.3合并版收口计划.md)

---

## 闸门

- [x] 本地合并版收口 + C 验证通过
- [x] Tag `nxin-v0.6.3-merge.1`（指向 `1a0fce53`）
- [x] PR-1 分支 `contrib/redis-cluster` 已开、已 push、PR 已提交
- [x] PR-2 SvgIcon 分支启动（与 PR-1 无代码依赖，可并行；待 PR-1 合入后开正式 PR 亦可）
- [x] PR-2 SvgIcon → 已开 PR（可与 Redis 并行）
- [x] PR-3 色值接入分支已提交并 push

---

## Phase 1: PR-1 Redis 集群（部署完整集）

**分支:** `contrib/redis-cluster` ← `upstream/main` @ `9733bd28` + commit `9c298b24`

### Task 1: 开分支

- [x] `git checkout -b contrib/redis-cluster upstream/main`

### Task 2: 移植双模式 + 保留 TLS

- [x] container / task / inspector / compose / Helm / `.env.example` / `docs/redis-cluster.md`
- [x] `go test ./internal/router/ -run GetAsynqRedisConnOpt`
- [x] `go build ./cmd/server`
- [x] Commit `9c298b24`
- [x] Push `origin contrib/redis-cluster` + PR 已提交

---

## Phase 2: PR-2 SvgIcon（仅核心）

**分支:** `contrib/svg-icon` ← `upstream/main` @ `a729bc12`

- [x] 新增 `frontend/src/components/icons/{SvgIcon.vue,registry.ts,index.ts}`
- [x] 首批替换 menu / AgentSelector / AgentStreamDisplay / deepThink / Input-field
- [x] **不含** 新增 `tdesign-icon-offline`（上游已有离线护栏，本 PR 不碰）
- [x] type-check + style test 通过
- [x] Commit + push `origin/contrib/svg-icon`
- [x] 开 GitHub PR（已手动提交）

---

## Phase 3: PR-3 色值接入

**分支:** `contrib/td-color-tokens` @ `ed42f42a` ← `upstream/main`

- [x] 硬编码 → `var(--td-*)` / `color-mix`，fallback 上游绿 `#07c05f`
- [x] 不上农信蓝 `theme.css` 色板
- [x] 文档 `docs/td-color-tokens.md`
- [x] Commit + push `origin/contrib/td-color-tokens`
- [ ] 开 GitHub PR

---

## 推送 Tag（可选）

```powershell
git push origin nxin-v0.6.3-merge.1
```

仅当你确认要备份到 Fork 时再执行。
