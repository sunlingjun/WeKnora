# 2026-07-30 0.7.0 个性开发保留基线核对

> 核对对象：  
> - **0.7.0**：`nxin-v0.7.0-stable.2` = `aabe37d1`（当前 `stable/2026-07-20-nxin-v0.7.0`）  
> - **0.7.1**：`stable/2026-07-30-nxin-v0.7.1`（含 cherry-pick 后的 tip，见 §3）

## 1. 结论

| 线 | 结论 |
|----|------|
| **0.7.0 稳定线** | P0/P1 个性基线齐全（含 CAS、共享 KB、构建收口、聊天引用图灌水） |
| **0.7.1（核对前）** | 缺 **`aabe37d1` 聊天多 KB 引用图灌水**；其余 P0 大体保留 |
| **0.7.1（核对后）** | 已 `cherry-pick aabe37d1` → `da5d5f8b`；本地探针 18/18 PASS |

## 2. P0 核对表（0.7.1 tip @ `da5d5f8b`）

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | CAS 路由 `validateSession`（非 Lite 禁止直跳 `/login`） | PASS |
| 1b | 广场路由 + members 在 `:kbId` 前 | PASS |
| 1c | 网页导入 title | PASS |
| 1d | 卡片品牌色（KB/Agent/Org 无硬编码绿/蓝） | PASS |
| 1e | ZSK / Nxin 品牌文案 | PASS |
| 1f | `kbEditorNavGroups` `members`+`share` | PASS |
| 1f2 | members i18n 四语 | PASS（此前单测 7/7） |
| 1g | SharedKB `uuid.New()` | PASS |
| 1h | joinedViaPlaza 可见性 | PASS |
| 1i | CAS `X-Tenant-ID` resolve | PASS |
| 1j | CAS `/files` allowlist | PASS |
| 1k | Stream `UniversalClient` | PASS |
| 1l | `cas_local_password` | PASS |
| 2–3 | CAS 公开路径 / 退出 | PASS |
| 4–5 | CAS 后端 / KB 静态路由顺序 | PASS |
| 6–7 | CORS / open_retrieve | PASS |
| 8 | sockopt unix/windows | PASS |
| 9–10 | api-base / env `VITE_APP_CAS` | PASS |
| 11–13 | Dockerfile GOPROXY+DuckDB、LOAD-first、`CAS_ENVIRONMENT` | PASS |
| 14 | Redis Cluster asynq | PASS |
| 15 | ParadeDB `0.22.2` | PASS |
| **额外** | `aabe37d1` 聊天多 KB `/files` 灌水 | **已补入 0.7.1** |

## 3. 曾发现的遗漏（已处理）

```
aabe37d1 fix(chat): 会话多 KB 上下文灌水，修复共享库聊天引用图 403
  → cherry-pick 到 stable/2026-07-30-nxin-v0.7.1 为 da5d5f8b
  涉及：security.ts、chat 组件、config.go（AllowedPathGlobs 不硬编码仅 /api/v1/*）
```

说明：`OrganizationList` 硬编码绿色在当前 tip 上为 **0**（此前误报/中间态）；`color-mix` 次数少于 0.7.0 因官方重写 org UI，属预期。

## 4. 0.7.0 vs 0.7.1 定制文件

| 文件 | 相对 0.7.0-stable.1 | 说明 |
|------|---------------------|------|
| `auth.go` / `cas_auth.go` / `config.yaml` | 保留 NXIN | X-Tenant、`/files` 在 |
| `Dockerfile.app` / `duckdb.go` / compose | 无业务回退 | 构建收口仍在 |
| `stream/*` | 保留 | UniversalClient |
| `shared_kb.go` / router 静态路由 | 保留 | |
| `security.ts` 等 | **0.7.1 已补** | 来自 stable.2 |

## 5. 建议动作

1. 将 0.7.1 tip 打成新 Tag：`nxin-v0.7.1-stable.2`（或 `-f` 更新 `stable.1`，需团队约定）  
2. 0.7.0 侧确认 `nxin-v0.7.0-stable.2`（`aabe37d1`）已推远端  
3. 联调仍测：CAS 切空间、共享库聊天图、图谱、库 70→74  
