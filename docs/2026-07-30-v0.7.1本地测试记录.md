# 2026-07-30 WeKnora-slj v0.7.1 本地测试记录

> 分支/Tag：`stable/2026-07-30-nxin-v0.7.1` / `nxin-v0.7.1-stable.1`  
> 环境：本机 Windows，未接测试/生产库与 CAS 实网。

## 已通过（证据）

| 项 | 结果 |
|----|------|
| `go build ./cmd/server` | exit 0（产出 `server-test-0.7.1.exe`） |
| `go test` middleware/stream/utils/router/types/application/service/handler | 全 ok |
| 前端 `kbEditorNavGroups` + `kbMembersI18n` | 7/7 pass |
| `npm run type-check`（vue-tsc） | exit 0 |
| `npm run build` | exit 0（chunk 体积告警可忽略） |
| P0 定制静态核对 | CAS X-Tenant、`/files`、Stream UniversalClient、`CAS_ENVIRONMENT`、Neo4j、Dockerfile GOPROXY/DuckDB、`/user`+`/shared` 路由均在 |

## 测试中修复并纳入本 Tag 的提交

- `open_retrieve_auth_test`：Auth 补齐 TenantAPIKey 参数
- `vectorstore_test` guard DDL：补 `visibility` 等共享库列
- i18n：去掉 organization 下与对象冲突的 string `members`
- `sessionSidebarBuckets`：options 补 `includeApiBucket`
- `SystemSettings`：补 `nextTick` import
- `card-list-badge`：`ListCardSectionKey` 含 `joinedShared`/`tenantReadonly`
- `kbMembersI18n` 测试：兼容 CRLF

## 尚未覆盖（需环境）

- [ ] 本机/测试 compose 起全栈 + CAS 登录
- [ ] 切工作空间（X-Tenant-ID）
- [ ] 聊天引用图 `/files`
- [ ] Redis Cluster SSE
- [ ] GraphRAG（Neo4j）检索
- [ ] 共享 KB 广场/成员/离开
- [ ] 库迁移 70→74
- [ ] Langfuse env 核查（若启用则 OTLP）

## 结论

**编译与单测/前端构建已绿**；可进入联调与测试环境部署验证。通过后再推送 `nxin-v0.7.1-stable.1`。
